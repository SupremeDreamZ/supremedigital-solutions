{background:var(--lime-hot)}
.game-card{box-shadow:0 24px 80px rgba(0,0,0,.72)}

.ending{
  min-height:0;
  padding:clamp(2.5rem,6vw,4.5rem) clamp(1rem,4.5vw,2.6rem) clamp(2.2rem,5vw,3.5rem);
  display:block;
  background:transparent;
}
.ending-heading{padding-bottom:1.2rem;border-bottom:4px solid #34372f}
.ending-grid{display:grid;grid-template-columns:1.08fr .92fr;gap:1.7rem;align-items:center;padding-top:1.5rem}
.ending-art{min-height:470px}.ending-copy p{line-height:1.55;color:#c1baae;font-weight:700}.ending-stats{display:grid}.ending-stats span{background:#d1c1a5;color:#141610;border:0;transform:rotate(-.5deg);font-weight:900;clip-path:polygon(2% 3%,97% 0,100% 91%,92% 100%,0 95%)}.ending-stats span:nth-child(2){transform:rotate(.45deg);background:#bca98c}.ending-stats b{color:#3e5800}.ending-note{position:absolute;right:6%;top:7%;z-index:4;padding:.55rem .7rem;background:var(--lime);color:#10120e;font:900 1rem/.88 Impact,sans-serif;transform:rotate(4deg);clip-path:polygon(3% 4%,95% 0,100% 91%,92% 100%,0 95%)}
footer{
  width:min(100%,864px);
  padding:2rem clamp(1rem,4.5vw,2.6rem) calc(2rem + var(--safe-bottom));
  border-top:5px solid #34372f;
  background:#050605;
  grid-template-columns:auto 1fr;
}

@media(max-width:760px){
  .arcade-poster-header{grid-template-columns:1fr auto}.arcade-warning{min-width:100px;font-size:.62rem;padding:.6rem .45rem}.arcade-poster-body{grid-template-columns:1fr;gap:.75rem}.arcade-art{min-height:360px;border-left:0;border-top:3px solid #34372f;padding-top:.75rem}.hero-crop{width:min(315px,90%)}.loadout article{min-height:128px}.arcade-launch-strip{display:grid}.arcade-actions{display:grid;width:100%}.ending-grid{grid-template-columns:1fr}.ending-art{min-height:390px}.ending-copy{padding-top:.3rem}
}
@media(max-width:430px){
  .arcade-intro,.ending{padding-left:.85rem;padding-right:.85rem}.arcade-poster-header h2,.ending-heading h2{font-size:clamp(3rem,16.5vw,4.8rem)}.arcade-warning{position:absolute;right:.75rem;top:4.2rem;opacity:.92}.arcade-poster-header{display:block;padding-right:5.8rem}.scribble-line{font-size:.82rem}.arcade-deck-copy{font-size:.76rem}.loadout{gap:.42rem}.loadout article{min-height:116px;padding:.55rem;grid-template-columns:40px 1fr}.loadout-icon{width:38px;height:38px;transform:scale(.84);transform-origin:top left}.loadout strong{font-size:.84rem}.loadout small{font-size:.47rem}.loadout article::after{font-size:.4rem!important}.arcade-art{min-height:330px}.arcade-target-label{font-size:.82rem}.arcade-launch-strip{padding-top:.85rem}.arcade-options{font-size:.58rem;gap:.6rem}.ending-art{min-height:320px}footer{grid-template-columns:1fr}
}
@media(max-width:900px) and (orientation:landscape){
  .game-hud{grid-template-columns:auto auto auto 1fr 1fr auto auto auto}.hud-ticket{min-width:58px;padding:.18rem .3rem}.game-help{top:auto;bottom:calc(.45rem + var(--safe-bottom));max-width:42%}.touch-actions button{min-width:92px;min-height:42px}
}

/* REVIEW LOOP PASS 6 — make the extension read like the next printed panel, not a landing-page appendix. */
.arcade-intro{
  padding:clamp(1.8rem,4.2vw,2.8rem) clamp(.9rem,4vw,2.2rem) clamp(1.5rem,3.6vw,2.4rem);
  background:
    linear-gradient(90deg,transparent 0 49.7%,rgba(230,221,204,.045) 49.8% 50.2%,transparent 50.3%),
    radial-gradient(circle at 83% 22%,rgba(167,211,36,.12),transparent 27%),
    #070806;
}
.arcade-intro::after{
  content:"";
  position:absolute;
  z-index:-1;
  inset:22% 0 auto;
  height:4px;
  background:#35372f;
  box-shadow:0 430px 0 #35372f;
  opacity:.88;
}
.arcade-poster-header{
  grid-template-columns:minmax(0,1.15fr) minmax(150px,.85fr);
  min-height:250px;
  align-items:center;
  padding:0 0 1rem;
  border-bottom:5px solid #35372f;
}
.arcade-poster-header::after{
  content:"";
  grid-column:2;
  grid-row:1;
  justify-self:end;
  width:min(240px,92%);
  aspect-ratio:1;
  border:2px solid rgba(167,211,36,.42);
  border-radius:50%;
  box-shadow:0 0 0 18px rgba(167,211,36,.025);
  background:
    radial-gradient(circle,rgba(167,211,36,.13),transparent 60%),
    repeating-conic-gradient(from 0deg,rgba(167,211,36,.24) 0 2deg,transparent 2deg 42deg);
  opacity:.72;
}
.arcade-poster-header>div:first-child{position:relative;z-index:2}
.arcade-poster-header h2,.ending-heading h2{
  font-size:clamp(3.5rem,9vw,5.8rem);
  max-width:8ch;
}
.arcade-warning{position:absolute;z-index:4;right:1.2rem;top:1rem;min-width:118px}
.arcade-poster-body{padding:1rem 0;gap:1rem}
.arcade-deck-copy{
  padding:.7rem .8rem;
  margin:0 0 .75rem;
  background:#171a13;
  border-left:6px solid var(--lime);
  color:#d4ccbe;
  clip-path:polygon(1% 3%,98% 0,100% 91%,95% 100%,0 96%);
}
.loadout{gap:.45rem}
.loadout article{
  min-height:132px;
  opacity:.98;
  filter:saturate(.78) brightness(.92);
  cursor:pointer;
  outline:0;
}
.loadout article::before{
  content:"";
  position:absolute;
  inset:7px;
  border:1px dashed rgba(29,32,26,.24);
  pointer-events:none;
}
.loadout article:hover,.loadout article:focus-visible{
  filter:none;
  transform:translateY(-3px) rotate(-.45deg);
  box-shadow:0 9px 18px rgba(0,0,0,.38);
}
.loadout article:focus-visible{outline:3px solid var(--lime-hot);outline-offset:3px}
.loadout article:not(.unlocked)::after{content:'TAP TO FIND PROOF CARD';font-weight:900}
.loadout article.unlocked::after{content:'LOCKED // READY'}
.arcade-art{min-height:390px;overflow:hidden;background:radial-gradient(circle at 50% 50%,rgba(167,211,36,.06),transparent 58%)}
.hero-crop{width:min(330px,88%)}
.arcade-target-label{left:5%;top:7%}
.enemy-stamp{right:4%;bottom:8%}
.validation-path{
  display:grid;
  grid-template-columns:auto 1fr auto 1fr auto 1fr auto;
  align-items:center;
  gap:.45rem;
  padding:.65rem .2rem .9rem;
  color:#8fae30;
  font:900 .58rem/1 Arial,sans-serif;
  letter-spacing:.09em;
}
.validation-path i{height:2px;background:linear-gradient(90deg,var(--lime),rgba(167,211,36,.12));position:relative}
.validation-path i::after{content:'>';position:absolute;right:-2px;top:-6px;color:var(--lime)}
.arcade-launch-strip{padding-top:.9rem}

.game-edge-copy{
  position:absolute;
  z-index:9;
  top:50%;
  color:rgba(199,249,61,.2);
  font:900 .55rem/1 ui-monospace,monospace;
  letter-spacing:.18em;
  pointer-events:none;
  writing-mode:vertical-rl;
}
.edge-left{left:.25rem;transform:rotate(180deg) translateY(50%)}
.edge-right{right:.25rem;transform:translateY(-50%)}
.touch-controls{height:35%}
.touch-move{
  width:50%;
  height:60%;
  bottom:calc(.7rem + var(--safe-bottom));
  background:rgba(6,8,5,.1);
  border-color:rgba(230,221,204,.22);
  color:rgba(230,221,204,.28);
  animation:touchHintFade 4.2s ease 1.8s forwards;
}
.touch-actions{bottom:calc(.7rem + var(--safe-bottom));gap:.5rem}
.touch-actions button{min-width:92px;min-height:48px}
@keyframes touchHintFade{
  to{border-color:rgba(230,221,204,.07);color:rgba(230,221,204,.08);background:transparent}
}

@media(max-width:760px){
  .arcade-poster-header{min-height:215px;grid-template-columns:1fr 42%}
  .arcade-poster-header h2{font-size:clamp(3.25rem,14vw,4.5rem)}
  .arcade-poster-header::after{width:min(190px,100%)}
  .arcade-warning{right:.7rem;top:.7rem;min-width:92px}
  .arcade-poster-body{grid-template-columns:1fr}
  .arcade-art{min-height:265px;border-top:4px solid #34372f}
  .hero-crop{width:min(245px,76%)}
  .loadout article{min-height:112px}
  .validation-path{font-size:.48rem;gap:.28rem}
}
@media(max-width:430px){
  .arcade-intro{padding-top:1.3rem}
  .arcade-poster-header{min-height:190px;padding-right:0;grid-template-columns:1fr 38%}
  .arcade-poster-header h2{font-size:clamp(2.9rem,15.2vw,4rem)}
  .arcade-warning{display:none}
  .arcade-poster-header::after{opacity:.52}
  .scribble-line{max-width:19rem}
  .loadout article{min-height:104px;padding:.5rem;grid-template-columns:36px 1fr}
  .loadout-icon{transform:scale(.76)}
  .arcade-art{min-height:235px}
  .hero-crop{width:min(220px,72%)}
  .arcade-target-label{font-size:.7rem}
  .enemy-stamp{font-size:.62rem}
  .validation-path span:nth-of-type(2),.validation-path span:nth-of-type(3){display:none}
  .validation-path{grid-template-columns:auto 1fr 1fr auto}
  .validation-path i:nth-of-type(2){display:none}
  .game-help{max-width:62%;padding:.42rem .55rem;border-left-width:5px}
  .game-help span{font-size:.56rem}.game-help small{font-size:.43rem}
  .touch-move{left:.45rem;width:52%;height:54%}
  .touch-actions{right:.45rem}
  .touch-actions button{min-width:86px;min-height:45px;font-size:.52rem}
}
@media(orientation:landscape) and (max-height:620px){
  .touch-controls{height:48%}
  .touch-move{height:58%;bottom:calc(.35rem + var(--safe-bottom))}
  .touch-actions{bottom:calc(.35rem + var(--safe-bottom))}
}

/* REVIEW LOOP PASS 7 — remove accidental focus artifact and tighten mobile game chrome. */
.poster-zone:focus{outline:none}
@media(max-width:600px){
  .game-hud{
    grid-template-columns:64px minmax(0,1fr) minmax(0,1fr) 38px 38px 38px;
    grid-template-rows:auto auto;
    column-gap:.28rem;
    row-gap:.28rem;
    padding:calc(.42rem + var(--safe-top)) .38rem .55rem;
  }
  .game-hud>.hud-ticket:first-child{grid-column:1;grid-row:1;margin-left:.18rem;min-width:0;padding:.25rem .32rem;transform:rotate(-.25deg)}
  .game-hud>div:nth-child(2),.game-hud>div:nth-child(3){display:none}
  .game-hud>.hud-meter:nth-child(4){grid-column:1/4;grid-row:2}
  .game-hud>.ash-meter{grid-column:4/7;grid-row:2}
  #pauseGame{grid-column:4;grid-row:1}
  #fullscreenGame{grid-column:5;grid-row:1}
  #muteGame{grid-column:6;grid-row:1}
  .game-hud button{width:36px;height:32px}
  .hud-ticket strong{font-size:.84rem}
  .game-help{top:calc(4.55rem + var(--safe-top))}
  .touch-move{animation:touchHintFade 1.4s ease .55s forwards;border-color:rgba(230,221,204,.15);color:rgba(230,221,204,.2)}
}

/* REVIEW LOOP PASS 8 — brand the gate, menu, and detail cards with the supplied poster language. */
.age-paper{position:relative;padding-top:1.15rem}
.age-logo,.menu-brand{
  color:var(--red);
  font:900 italic clamp(2rem,9vw,3.7rem)/.86 Impact,Haettenschweiler,'Arial Narrow Bold',sans-serif;
  letter-spacing:-.03em;
  text-shadow:1px 1px 0 #111,0 0 1px #fff;
}
.age-scribble{
  margin:.25rem 0 .75rem!important;
  color:#567300;
  font:900 .72rem/1 'Comic Sans MS','Bradley Hand',cursive!important;
  letter-spacing:.035em;
  transform:rotate(-1deg);
}
.age-scribble::after{content:"";display:block;width:190px;height:4px;margin-top:.25rem;background:var(--lime);clip-path:polygon(0 34%,14% 10%,28% 44%,43% 18%,58% 40%,73% 12%,88% 39%,100% 20%,100% 70%,84% 52%,68% 79%,50% 50%,33% 77%,16% 54%,0 73%)}
.age-paper h1{margin-top:.3rem}

.poster-menu{
  background:
    linear-gradient(rgba(5,6,5,.92),rgba(5,6,5,.98)),
    radial-gradient(circle at 70% 15%,rgba(167,211,36,.18),transparent 30%);
  padding-top:calc(4.2rem + var(--safe-top));
  gap:.4rem;
}
.menu-brand{margin-bottom:.2rem;font-size:clamp(2.2rem,10vw,4.3rem)}
.menu-note{margin:0 0 1.1rem;color:var(--lime);font:900 .68rem/1.2 'Comic Sans MS',cursive;letter-spacing:.06em;transform:rotate(-.6deg)}
.poster-menu a{
  position:relative;
  width:max-content;
  max-width:94vw;
  padding:.22rem .2rem .35rem 0;
  color:var(--paper);
  font:900 clamp(2.35rem,10vw,5.7rem)/.78 Impact,Haettenschweiler,'Arial Narrow Bold',sans-serif;
  letter-spacing:-.025em;
  text-transform:uppercase;
  text-shadow:1px 0 #a59d8e,-1px 0 #4c4a44;
}
.poster-menu a::after{content:"";position:absolute;left:0;right:0;bottom:.05rem;height:2px;background:#30332b;transform:scaleX(.16);transform-origin:left;transition:transform .18s ease,background .18s ease}
.poster-menu a:hover,.poster-menu a:focus-visible{color:var(--lime-hot);outline:none}
.poster-menu a:hover::after,.poster-menu a:focus-visible::after{transform:scaleX(1);background:var(--lime-hot)}
.menu-close{background:var(--paper);color:#10120e;border:0;clip-path:polygon(3% 4%,96% 0,100% 92%,91% 100%,0 95%);font-weight:900}

.strain-dialog{
  --strain-accent:var(--lime);
  position:relative;
  background:#d4c3a6;
  color:#171914;
  border:0;
  border-top:9px solid var(--strain-accent);
  padding:clamp(1.4rem,5vw,2.6rem);
  clip-path:polygon(2% 0,12% 1.8%,24% .3%,37% 1.6%,50% .2%,64% 1.7%,78% .4%,91% 1.8%,100% 0,98.5% 14%,100% 29%,98% 46%,100% 62%,98% 80%,100% 96%,89% 98.5%,75% 97%,62% 100%,48% 97%,34% 99.5%,19% 97%,2% 99%,0 86%,1.8% 70%,0 53%,2% 37%,0 20%);
}
.strain-dialog .micro{color:#496100}
.strain-dialog h2{font-family:Impact,Haettenschweiler,'Arial Narrow Bold',sans-serif;letter-spacing:-.02em;text-transform:uppercase;text-shadow:1px 0 #8e836f}
.strain-dialog p{color:#33362e;font-weight:700}
.strain-dialog .dialog-close{background:#171914;color:var(--paper);border:0;clip-path:polygon(4% 1%,96% 5%,100% 91%,91% 100%,0 95%)}
.strain-tags span{background:var(--strain-accent);color:#11130f}

/* REVIEW LOOP PASS 9 — landscape-safe age gate. */
@media(orientation:landscape) and (max-height:620px){
  .age-gate{place-items:start center;overflow:auto;padding:.45rem}
  .age-paper{width:min(96vw,760px);max-height:calc(100dvh - .9rem);overflow:auto;padding:.65rem 1.1rem .75rem;clip-path:polygon(0 3%,4% 0,9% 2%,15% 0,22% 2%,30% 0,39% 2%,49% 0,59% 2%,69% 0,79% 2%,89% 0,96% 2%,100% 0,99% 22%,100% 46%,99% 69%,100% 96%,92% 100%,80% 98%,67% 100%,54% 98%,41% 100%,28% 98%,15% 100%,0 97%,1% 75%,0 52%,1% 28%)}
  .age-paper .micro{margin-bottom:.15rem;font-size:.55rem}
  .age-logo{font-size:1.9rem}
  .age-scribble{display:none}
  .age-paper h1{font-size:clamp(2.25rem,8vh,3.1rem);line-height:.82;margin:.2rem 0 .45rem}
  .age-paper>p:not(.micro):not(.age-scribble){font-size:.78rem;line-height:1.3;margin:.2rem 0 .55rem}
  .age-actions{margin-top:.45rem;display:flex;flex-wrap:nowrap}
  .age-actions .paper-button{width:auto;min-width:150px;padding:.62rem .9rem}
  .age-paper small{margin-top:.45rem;font-size:.62rem}
}
